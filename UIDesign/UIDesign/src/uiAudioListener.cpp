#include "uiAudioListener.h"


void AudioListener::PropagateTransform(const Transform2D& newTransform)
{
  // Listener::setPosition(newTransform.position.x + m_offset->x, 0, newTransform.position.y + m_offset->y);
}